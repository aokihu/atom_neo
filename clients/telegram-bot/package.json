{
  "name": "atom-neo-telegram-bot",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "start": "bun run src/main.ts",
    "build": "bun build src/main.ts --compile --outfile telegram-bot",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "dependencies": {
    "telegramify-markdown": "^1.3.3"
  }
}
